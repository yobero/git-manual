<!doctype HTML>
<html>
    <head>
        <title>git rebase</title>
        <meta charset="UTF-8">
        <link href="../css/style.css" rel="stylesheet" type="text/css">
    </head>

    <body>
        <div id="titre"> 
            <h1> <a href="../index.html"> <img src="../images/logo.png" alt="logo" > </img> </a> <br>Nom de la commande</h1>
        </div>
        <div id="nav">
                <nav>
                    <ul>
                        <li><a href="../index.html">Accueil</a></li>
                    </ul>
                </nav>
        </div>
        <div id="corps">
        <section class="descriptif">
                <p>git rebase</p>
              <!--Description de la commande -->
        </section>

        <section class="affichage">
            <h4>Syntaxe :</h4>
                        <code>Syntaxe d'utilisation de la commande</code>

                        <div class="code">
                            <code>Exemple d'affichage dans le terminal</code>
                        </div>
        </section>
          <section class="descriptif">
                <p>git rebase --abort</p>
              <!--Description de la commande -->
        </section>

        <section class="affichage">
            <h4>Syntaxe :</h4>
                        <code>Syntaxe d'utilisation de la commande</code>

                        <div class="code">
                            <code>Exemple d'affichage dans le terminal</code>
                        </div>
        </section>
          <section class="descriptif">
                <p>git rebase --onto <new base></p>
              <!--Cette commande permet de redéfinir un nouveau point d'origine pour tout les nouveaux commits.
              Ce nouveau point peut être initialisé à partir de n'importe quelle commit valide.-->
        </section>

        <section class="affichage">
            <h4>Syntaxe :</h4>
                        <code>Syntaxe d'utilisation de la commande</code>

                        <div class="code">
                            <code>First, rewinding head to replay your work on top of it...
Applying: F2a
Applying: F2b</code>
                        </div>
        </section>
          <section class="descriptif">
                <p>git rebase -i</p>
              <!--Cette commande présente une liste de tous les commits qui sont sur le point d'être "rebase".
              Cela permet à l'utilisateur de modifier la liste avec qu'elle ne soit "rebase". Cette commande
              peut également être utilisée pour séparer certains commits.-->
        </section>

        <section class="affichage">
            <h4>Syntaxe :</h4>
                        <code>Syntaxe d'utilisation de la commande</code>

                        <div class="code">
                            <code>pick 01d1124 Adding license
pick 6340aaa Moving license into its own file
pick ebfd367 Jekyll has become self-aware.
pick 30e0ccb Changed the tagline in the binary, too.

# Rebase 60709da..30e0ccb onto 60709da
#
# Commands:
#  p, pick = use commit
#  e, edit = use commit, but stop for amending
#  s, squash = use commit, but meld into previous commit
#
# If you remove a line here THAT COMMIT WILL BE LOST.
# However, if you remove everything, the rebase will be aborted.</code>
                        </div>
        </section>
          <section class="descriptif">
                <p>git rebase --continue </p>
              <!--Description de la commande -->
        </section>

        <section class="affichage">
            <h4>Syntaxe :</h4>
                        <code>Syntaxe d'utilisation de la commande</code>

                        <div class="code">
                            <code>Exemple d'affichage dans le terminal</code>
                        </div>
        </section>


    </div>
    <footer>
    <p> © Groupe d'IN202 2014-2015 UVSQ - Lebataclan, LowD, Mr-Nico-972</p>
    </footer>
    </body>

</html>
